let people: string[] = ["MB", "MAB", "VMB", "AOB", "PB"]
console.log(people)

let numbers: Array<number> = [11, 2, 4, -1]
console.log(numbers)

let info: (string | number)[] = ["web3", 2023]
console.log(info)

let values: Array<string | boolean> = [true, "okey"]
console.log(values)

let subjects: Array<string>;
subjects = ["finance", "math", "ai"]

for (let i = 0; i < subjects.length; i++) {
    console.log(subjects[i])
}
